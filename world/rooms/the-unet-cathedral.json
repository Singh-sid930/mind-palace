{
  "id": "the-unet-cathedral",
  "name": "The U-Net Cathedral",
  "wing": "diffusion",
  "size": "grand",
  "order": 5,
  "exhibits": [
    {
      "id": "the-cathedral",
      "type": "artifact",
      "title": "The Cathedral of Descent and Return",
      "prop": "statue",
      "scale": 1.5,
      "text": "A cathedral shaped like the letter U, carved so that you may only reach the far door by first walking DOWN into the crypt and then climbing back UP the other side. This is the U-Net — the network that learned to denoise images.\n\nRemember the law from the earlier chambers of this wing: the diffusion MATH does not change when you move from a single number to a whole image. The forward-noising equation x_t = sqrt(a_bar_t) * x_0 + sqrt(1 - a_bar_t) * eps is applied ELEMENT-WISE across the array; eps is now a full field of noise, one N(0,1) draw per pixel, the same shape as the image, while a_bar_t stays a single scalar (one noise level shared by every pixel). Nothing in the algebra is new.\n\nWhat IS new is the sorcerer who must read the noise. For a scalar a tiny MLP sufficed. For an image the network must take a noisy field of shape (H, W, C) and hand back predicted noise of the EXACT same shape (H, W, C) — a per-pixel prophecy, net(x_t, t) -> eps_pred, (256, 256, 3) in, (256, 256, 3) out. And to make that prophecy well it must understand the image at every scale at once: the local grain of a texture AND the global architecture of the whole scene. Same-shape-in-same-shape-out, plus multi-scale sight. Those two demands, together, are the exact riddle the U-shape answers.\n\nDESCEND the left arm — the ENCODER. At each landing it halves the spatial resolution and doubles the channels: 256 -> 128 -> 64 -> 32, each floor smaller in extent but richer in meaning. Spatial detail is traded away for semantic depth; the image is being compressed into understanding.\n\nStand in the CRYPT — the BOTTLENECK. Here the map is smallest and deepest, the field of view widest. Every position can, in principle, feel every other. This is the seat of maximum context and minimum spatial detail: the network knows WHAT the picture is, having thrown away much of WHERE.\n\nCLIMB the right arm — the DECODER. Now it reverses the trade, doubling spatial resolution back up 32 -> 64 -> 128 -> 256 and shedding channels, reconstructing the full-resolution field of predicted noise until output shape equals input shape. The symmetry of the two arms is precisely why the shapes match at the door.\n\nBut a decoder climbing alone from the crypt would produce only a blur — it holds deep meaning but has forgotten fine location. So the cathedral is strung with SKIP CONNECTIONS: flying buttresses that carry each encoder landing's high-resolution feature map straight across the nave to the decoder landing at the same height. The decoder at every level fuses two things — global understanding risen from below, and crisp detail thrown across the skip — and only then does the U-Net see both the forest and the leaf.\n\nEach landing of this cathedral is not a bare room but a small rite in fixed order: a ResNet convolution reads local features, the timestep t is injected so the block knows which noise level it is undoing, self-attention lets far-apart positions confer (only where the map is small enough to afford it), and cross-attention lets the image listen to the words of a prompt. Study the plinths around you to learn each in turn. Together they are the beating heart of Stable Diffusion."
    },
    {
      "id": "same-math-new-sorcerer",
      "type": "plaque",
      "prop": "pedestal",
      "title": "The Same Spell, a Greater Sorcerer",
      "text": "A common misreading: that moving from denoising a number to denoising a picture demands new mathematics. It does not. Scalar and image obey the identical equation, x_t = sqrt(a_bar_t) * x_0 + sqrt(1 - a_bar_t) * eps — for the scalar these are single numbers, for the image they are 256x256x3 arrays and the operation runs element-wise. eps grows into a full array of noise (one standard-normal draw per pixel); a_bar_t remains one scalar, the same noise level for all pixels. Only the NETWORK is promoted. A scalar could be denoised by a tiny MLP. An image demands a map from a noisy field (H, W, C) to predicted noise of the same shape (H, W, C), made per pixel yet informed by the whole scene — local texture and global structure at once. 'Same shape in, same shape out, seen at many scales' is not a decorative wish; it is the design specification, and the U-Net is its answer."
    },
    {
      "id": "u-shape-diagram",
      "type": "diagram",
      "title": "The U-Shape and Its Flying Buttresses",
      "spec": "graph TD\n  I[\"noisy image H W C  +  t\"] --> E1[\"encoder 256\"]\n  E1 --> E2[\"encoder 128\"]\n  E2 --> E3[\"encoder 64\"]\n  E3 --> B[\"bottleneck 32 - deepest\"]\n  B --> D3[\"decoder 64\"]\n  D3 --> D2[\"decoder 128\"]\n  D2 --> D1[\"decoder 256\"]\n  D1 --> O[\"predicted noise H W C\"]\n  E3 -. skip .-> D3\n  E2 -. skip .-> D2\n  E1 -. skip .-> D1",
      "caption": "Down the left arm the encoder shrinks space and grows channels (256->128->64->32); the bottleneck holds maximum context; up the right arm the decoder grows space back (32->...->256). The dotted skips carry each level's high-resolution detail across to its mirror level. The two arms are symmetric, which is exactly why output shape equals input shape."
    },
    {
      "id": "cathedral-cross-section",
      "type": "image",
      "title": "Cross-Section of the Cathedral",
      "image": "world/assets/unet-cathedral.png",
      "caption": "The cathedral drawn in cross-section. Read it as a building: each block's HEIGHT encodes spatial resolution, its WIDTH the channel count. Descend the left arm and every landing halves the map (256->128->64) while doubling channels — space traded for meaning — until the crypt at 32x32x512 holds the widest field of view and the deepest understanding. Climb the right arm and the trade reverses, resolution doubling back to 256 so the predicted-noise field matches the input shape exactly. The luminous arcs are the flying-buttress SKIP connections: each encoder landing hands its high-resolution detail straight across the nave to the decoder landing of matching size, so the decoder fuses global meaning risen from the crypt with crisp local detail thrown across. Note where the rites sit: convolution does the cheap local work at every level, but self-attention and cross-attention (cost O(n^2)) are afforded only in the shrunken lower rooms near the crypt."
    },
    {
      "id": "the-buttresses",
      "type": "plaque",
      "prop": "lectern",
      "title": "The Buttresses — What Against Where",
      "text": "Downsampling is a bargain with a hidden cost. As the encoder shrinks 256 -> 128 -> 64 -> 32, it distils WHAT is in the image — a dog, a skateboard, a sky — but it quietly discards WHERE precisely each edge lay. Rise from the crypt with only that distilled meaning and the decoder must guess the fine positions; its output comes out soft and blurred. The skip connections are the remedy: each encoder level passes its feature map DIRECTLY, at full local resolution, to the decoder level of matching size (64 to 64, 128 to 128, 256 to 256). The decoder then combines global understanding risen from below with sharp detail handed across the skip. This is the same information-highway idea as ResNet's residual connections met in the Attention wing — a path that lets untouched detail flow straight through. Remove the skips and the U-Net still knows what it is drawing; it simply can no longer say exactly where. The difference between them is the difference between a crisp image and a blur."
    },
    {
      "id": "inside-the-block",
      "type": "plaque",
      "prop": "pedestal",
      "title": "Inside a Landing — Conv, Time, and the Familiar Gaze",
      "text": "Every landing performs three rites. FIRST, a ResNet convolution reads LOCAL features — edges, textures — cheaply, at all resolutions; this is the workhorse. SECOND, the timestep t is injected. The block must know WHICH noise level it is undoing, so t is turned into a vector by a sinusoidal embedding — the very positional-encoding trick from the basement's Wheel of Frequencies — and added into the features. THIRD, and only where the map is small, SELF-ATTENTION. This is EXACTLY the (n, n) attention from the Attention wing, now over spatial positions: flatten a 32x32xC feature map into a sequence of n = 1024 positions of dimension C, and let every position attend to every other. The score matrix is (1024, 1024) — a million entries, each pixel-position weighing every other. Crucially, attention only MIXES; it never changes shape. Flatten (32,32,C) -> (1024,C) is lossless rearrangement; (1024,1024) @ (1024,C) = (1024,C), same count, same dimension; reshape back to (32,32,C) is pure bookkeeping. The big score matrix is a consumed intermediate that never appears in the input or output shape. Why only at low resolution? Cost is O(n^2). At 32x32, n=1024 gives a tidy ~1M-entry matrix. At 256x256, n=65,536 gives roughly 4 BILLION entries — ruinous. So convolution does the cheap local work at high resolution, and attention is reserved for the crypt-near levels where global reasoning is affordable."
    },
    {
      "id": "cross-attention-steering",
      "type": "plaque",
      "prop": "lectern",
      "title": "The Second Gaze — Listening to the Prompt",
      "text": "A denoiser that only looked at itself could make a coherent picture of ANYTHING. To make the picture the one you ASKED for, a fourth rite is inserted right after self-attention: CROSS-ATTENTION between image and text. The prompt 'a corgi on a skateboard' is first turned into vectors by a text encoder (CLIP or T5), say shape (7, 768) — computed once and reused at every diffusion step and every U-Net level. Then the wiring, identical to the chamber of The Two Gazes: Q comes from the IMAGE positions (1024, d); K and V come from the TEXT tokens (7, d). The score Q.K^T is (1024, 7) — NOT square, because the two sides have different lengths — and the output (1024, 7) @ (7, d) = (1024, d) returns to image shape. Each image position asks 'which words are relevant to me?': skateboard-region patches attend to the token 'skateboard', dog-region patches to 'corgi'. This is the same image-into-text cross-attention you met in the Attention wing; here it is the steering wheel. Self-attention buys internal coherence; cross-attention buys agreement with the prompt. The two gazes together are what make text-to-image diffusion obey your words."
    },
    {
      "id": "block-order-diagram",
      "type": "diagram",
      "title": "The Four Rites of a Landing, in Order",
      "spec": "graph TD\n  A[\"ResNet conv - local features\"] --> B[\"inject timestep t - which noise level\"]\n  B --> C[\"self-attention - image to image - coherence\"]\n  C --> D[\"cross-attention - image to text - prompt steering\"]\n  D --> O[\"out - same shape as in\"]",
      "caption": "The full block, in fixed order: convolution for local features, timestep injection so the block knows the noise level, self-attention for spatial coherence (image looking at itself), then cross-attention for prompt steering (image listening to the text tokens). This four-step landing, stacked down and up the U, is the core of Stable Diffusion."
    }
  ]
}
